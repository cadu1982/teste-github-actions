name: Versioning Automation with SemVer

<<<<<<< HEAD
=======
on:
  workflow_call:
>>>>>>> 05967a9b37aa28fa7539d444ebaf019548ece7a7

# on: pull_request

jobs:
  Workflow-versioning:
    runs-on: ubuntu-latest

    steps:
     - name: Workflow version
       id: versioning
       uses: ./.github/actions/action.yml # BancoArbi/github-semantic-versioning-actions@v1.0.0

     - name: Create Release
       if: ${{ !startsWith(github.ref, 'refs/tags/') }}
       id: version
       uses: "marvinpinto/action-automatic-releases@latest"
       with:
         repo_token: "${{ secrets.GITHUB_TOKEN }}"
         title: ${{ steps.versioning.outputs.version }}
         automatic_release_tag: ${{ steps.versioning.outputs.version }}
         prerelease: false